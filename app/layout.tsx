import './globals.css';
export const metadata={title:'MediaFlow — Download Video & Audio',description:'Fast, simple media processing for direct public media URLs.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
